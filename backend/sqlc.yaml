version: "2"
sql:
  - engine: "postgresql"
    schema: "db/init.sql"
    queries: "internal/db/queries"
    gen:
      go:
        package: "dbgen"
        out: "internal/db/gen"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_empty_slices: true

