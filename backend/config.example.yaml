app:
  env: development
  serviceName: uni-ai-api
  version: dev

server:
  httpAddr: ":13010"

logging:
  level: info
  format: json
  accessLog: true
  slowRequestMs: 1000

postgres:
  dsn: "postgres://<user>:<password>@127.0.0.1:5432/uni_ai_api?sslmode=disable&search_path=public"
  maxConns: 10
  minConns: 1
  maxConnLifetime: 1h

redis:
  enabled: true
  addr: "127.0.0.1:6379"
  password: ""
  db: 0

urm:
  baseUrl: "http://127.0.0.1:6900"
  appKey: "UNI_AI_API"
  appSecret: "change-me"
  timeout: 10s

security:
  providerKeyMaster: "change-me-32-byte-minimum-secret"
  adminToken: "local-admin-token"
