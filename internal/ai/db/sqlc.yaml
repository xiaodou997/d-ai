version: "2"
sql:
  - engine: "postgresql"
    queries: "queries"
    schema: "../../db/init.sql"
    gen:
      go:
        package: "dbgen"
        out: "gen"
        sql_package: "pgx/v5"
        emit_json_tags: true
