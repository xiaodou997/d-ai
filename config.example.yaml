# D-AI 本地启动配置（不入仓：.gitignore 已忽略 config.yaml）。
# 生产用环境变量（DAI_*）注入，本文件仅本地 make dev 使用。
# 优先级：环境变量 > 本文件 > 代码默认值。

app:
  env: development

server:
  addr: ':19641'
  read_timeout: 30
  idle_timeout: 60

database:
  url: postgres://postgres:postgres@localhost:15432/dai?sslmode=disable

redis:
  addr: 127.0.0.1:16379

jwt:
  expiration: 2h
  refresh_expiration: 168h
  issuer: dai

security:
  # URM 与 AI 敏感配置的统一加密主密钥；生产环境填入原 URM 密钥
  secret_master_key: ''

storage:
  data_dir: data

legal:
  terms_version: '2026-07-19'
  privacy_version: '2026-07-19'

log:
  level: debug
