# 流式用量与结算规则

此前的局部修补方案已由 schema 42 的请求／结算重构替代。以 [验收记录](implementation/request-ledger-rebuild.md) 和 [切换手册](REQUEST_LEDGER_CUTOVER.md) 为准。

明确请求错误即使带 usage 也两层免收；客户端单纯断开不归为请求错误，只结算取消前已报告用量。立即取消上游，不排空流补 usage。没有计费依据不估算、不收费。执行事实、消费裁决、实际过账和交付中断分别记录，不能通过旧的成功／失败状态决定扣款。
